@model DataModelEntity.Entity.HarvestPlan

@{
    ViewData["Title"] = "Create";
}


<div class="col-12">
    <div class="card card-header sameheight-item">
        <a style="width:60px; height:30px" asp-action="Create" asp-controller="Students" class="btn btn-primary">
            <i class="fa fa-backward"></i>
        </a><h3>Create Training Plan</h3>
        <div class="card-header">
            <div class="row">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label class="control-label">Department</label>
                        <input asp-for="Depatment" class="form-control" />
                        <span asp-validation-for="Depatment" class="text-danger"></span>
                    </div>
                    <div class="form-group">
            <label  class="control-label">Select departmens</label>
            <select class="form-control" asp-for="GetTeacher.DepartmentId" asp-items="ViewBag.Department" />
        </div>
                    <div class="form-group">
                        <label class="control-label">Select Teacher</label>
                        <select asp-for="GetTeacher.Name" class="form-control" asp-items="ViewBag.TeacherId"></select>
                    </div>
                    @*<div class="form-group">
                        <label class="control-label">Select Subject code</label>
                        <select asp-for="GetSubject.SubjectCode" class="form-control" asp-items="ViewBag.SubjectId"></select>
                    </div>*@
                    <div class="form-group">
                        <label asp-for="GrupId" class="control-label"></label>
                        <select asp-for="GrupId" class="form-control" asp-items="ViewBag.GrupId"></select>
                    </div>
                    <a asp-controller="Grups" asp-action="Create" class="btn btn-primary">
                        Add facultet
                    </a>
                   
                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" />
                    </div>
                </form>
                </div>
            </div>
</div>
        </div>

            @section Scripts {
                @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
            }
