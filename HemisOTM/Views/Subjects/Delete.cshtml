@model DataModelEntity.Entity.Subject

@{
    ViewData["Title"] = "Delete";
}

<div class="col-12">
    <div class="card card-header sameheight-item">
        <a style="width:60px; height:30px" asp-action="Details" asp-controller="Subjects" asp-route-id="@Model.SubjectId" class="btn btn-primary">
            <i class="fa fa-backward"></i>
        </a><h3>Siz ushbu fanni o'chirishni xohlaysizmi? </h3>
        <div class="card-header">
            <dl class="row">
                <dt class="col-sm-10">
                    @Html.DisplayNameFor(model => model.SubjectCode)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.SubjectCode)
                </dd>
                <dt class="col-sm-10">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
            </dl>

        </div>

        <form asp-action="Delete">
            <input type="hidden" asp-for="SubjectId" />
            <input type="submit" value="Delete" class="btn btn-danger" /> 
           
        </form>
    </div>
</div>